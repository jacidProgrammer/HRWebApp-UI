// Runtime configuration. This file is replaced at container start (see docker/40-app-config.sh) with
// values from environment variables. During development it stays empty and the VITE_* variables apply.
window.__APP_CONFIG__ = window.__APP_CONFIG__ || {};
